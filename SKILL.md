---
name: worktrace
description: 提取指定日期的飞书工作聊天，围绕本人发言上下文做批量语义分析，生成事项级工作事件并写入本地 Excel。适用于需要从自己参与过讨论的会话中回顾工作进展、结果和关键沟通内容的场景。
---

# WorkTrace

## 用途

WorkTrace 用于从飞书聊天中提取指定日期的工作相关沟通内容，围绕本人发言锚点整理上下文，调用 Codex 做批量语义分析，并将结果写入本地 Excel 明细表，便于后续回顾、总结和生成日报素材。

## 使用方式

当用户要求回顾某一天的工作聊天、从飞书聊天生成工作事件记录、或将当天沟通整理成结构化事件时，使用本 skill。

优先按以下流程执行：

1. 确认目标日期。
2. 在仓库根目录执行 WorkTrace 的 Python 入口，而不是直接在 `SKILL.md` 中实现业务逻辑。
3. 让 Python 负责聊天抓取、窗口裁剪、批量组装、结果校验和 Excel 写入。
4. 仅在需要语义提取时调用 Codex 做批量分析。

## 约束

- 仓库根目录就是 skill 根目录，不使用单独的 `skill/` 子目录。
- 核心实现应放在 `src/`，测试放在 `tests/`，设计文档放在 `docs/`。
- 不要让 LLM 参与数据计算；统计或计算必须由 Python 或 Excel 公式完成。
- 原始聊天内容不应长期落盘，长期保留的只有结构化事件。

## 参考

- 详细设计：`docs/detailed-design.md`
- 项目说明：`README.md`
